{
  "name": "kitcheniq-2",
  "version": "0.1.0",
  "description": "KitchenIQ 2.0 foundation baseline",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "db:start": "supabase start",
    "db:stop": "supabase stop",
    "db:status": "supabase status",
    "db:reset": "supabase db reset",
    "test:db": "supabase test db",
    "test:auth:local": "npm run db:reset && npm run build && node scripts/f36-local-auth-integration.js",
    "test:security:local": "KITCHENIQ_ENVIRONMENT=automated_test npm run build && KITCHENIQ_ENVIRONMENT=automated_test node scripts/f37-local-security-integration.js",
    "test:reliability:local": "KITCHENIQ_ENVIRONMENT=automated_test npm run build && KITCHENIQ_ENVIRONMENT=automated_test node scripts/f40-local-reliability-integration.js",
    "test:observability:local": "KITCHENIQ_ENVIRONMENT=automated_test npm run build && KITCHENIQ_ENVIRONMENT=automated_test node scripts/f41-local-runtime-integration.js",
    "recovery:fixture": "npm run build && node scripts/f42-recovery.js fixture",
    "recovery:backup:local": "npm run build && node scripts/f42-recovery.js backup",
    "recovery:verify": "npm run build && node scripts/f42-recovery.js verify",
    "recovery:restore:test": "npm run build && node scripts/f42-recovery.js restore-test",
    "recovery:preflight": "npm run build && node scripts/f42-recovery.js preflight",
    "recovery:exercise:local": "npm run build && node scripts/f42-recovery.js exercise",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "KITCHENIQ_ENVIRONMENT=automated_test jest --coverage",
    "verify": "npm run typecheck && npm run test && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/big.js": "^7.0.0",
    "@types/jest": "29.5.3",
    "jest": "29.6.1",
    "supabase": "2.114.0",
    "ts-jest": "29.1.0",
    "typescript": "5.2.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.112.3",
    "big.js": "^7.0.1"
  }
}
